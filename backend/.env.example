# Senior Navigator Backend - Environment Variables

# OpenAI API Key (required for LLM features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI Model (optional)
# Default: gpt-4o-mini
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo
OPENAI_MODEL=gpt-4o-mini

# LLM Mode for GCP recommendations
# Options: 'off' (default), 'shadow' (log only), 'assist' (use LLM with high confidence)
# Default: 'off' (deterministic only)
FEATURE_GCP_LLM_TIER=off

# Memory Care Behavior Gate (optional)
# Options: 'off' (default), 'on'
# Default: 'off'
FEATURE_GCP_MC_BEHAVIOR_GATE=off

# Server Port (optional)
# Default: 3000
PORT=3000

# Allowed CORS Origins (optional, comma-separated)
# Default: http://localhost:4200,http://127.0.0.1:4200
# Production: https://yourapp.com,https://www.yourapp.com
ALLOWED_ORIGINS=http://localhost:4200,http://127.0.0.1:4200

# Node Environment (optional)
# Options: 'development', 'production', 'test'
# Default: 'development'
NODE_ENV=development

# Log Level (optional)
# Options: 'debug', 'info', 'warn', 'error', 'none'
# Default: 'info'
LOG_LEVEL=info

# LLM Confidence Threshold (optional)
# Range: 0.0 - 1.0
# Default: 0.7
CONFIDENCE_THRESHOLD=0.7
